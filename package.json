{
	"name": "prusalink-webhook",
	"module": "src/main.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun --watch src/main.ts",
		"build": "bun build src/main.ts --compile",
    "buildpi": "bun build src/main.ts --target=bun-linux-arm64 --compile"
	},
	"devDependencies": {
		"@types/bun": "^1.3.2"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"bun-sqlite-key-value": "^1.13.1"
	}
}
